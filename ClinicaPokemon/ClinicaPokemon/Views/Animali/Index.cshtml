@model IEnumerable<ClinicaPokemon.Models.Animali>

@{
    ViewBag.Title = "Index";
}

<div id="background">
    @*<div class="pb-2 my-4 shadow text-white d-flex justify-content-center align-items-center">
        <span class="mx-3 text-center">
            @Html.ActionLink("Aggiungi", "Create", new { }, new { @class = "text-dark text-decoration-none fw-bold" })
        </span>

        <h2 class="text-center fw-bold display-3 mx-3">Pokédex</h2>

        <span class="mx-3 text-center">
            @Html.ActionLink("Ricoveri", "Index", "Ricoveris", new { }, new { @class = "text-dark text-decoration-none fw-bold" })
        </span>
    </div>*@

    <div id="pokedex">
        <div id="carouselExample" class="carousel slide">
            <div class="carousel-inner">
                @for (int i = 0; i < Model.Count(); i++)
                {
                    var item = Model.ElementAt(i);
                    <div class="carousel-item @(i == 0 ? "active" : "")">
                        <img src="https://www.serebii.net/pokemon/art/041.png" class="d-block w-100" alt="@item.Nome">
                        <div class="pkm-description">
                            <p class="m-0 p-0">@item.Utenti.Cognome @item.Utenti.Nome</p>
                            <p class="m-0 p-0">Nome: @item.Nome</p>
                            <p class="m-0 p-0">Tipo: @item.Tipologia</p>
                            <p class="m-0 p-0">Colore: @item.Colore</p>
                            <p class="m-0 p-0">Data Nascita: @item.DataNascita.ToString("dd/MM/yyyy")</p>
                            <p class="m-0 p-0">Microchip: @(item.Microchip ? "Si" : "No")</p>
                            <p class="m-0 p-0">Numero Chip: @item.NrMicro</p>
                        </div>
                    </div>
                }
            </div>
            <button class="carousel-control-prev" type="button" data-bs-target="#carouselExample" data-bs-slide="prev">
                <i class="text-dark fs-1 bi bi-caret-left-fill"></i>
                <span class="visually-hidden">Previous</span>
            </button>
            <button class="carousel-control-next" type="button" data-bs-target="#carouselExample" data-bs-slide="next">
                <i class="text-dark fs-1 bi bi-caret-right-fill"></i>
                <span class="visually-hidden">Next</span>
            </button>
        </div>
    </div>


</div>




@*<table class="table">
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Utenti.Username)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Nome)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Tipologia)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Colore)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.DataNascita)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Microchip)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.NrMicro)
                </td>
                <td>
                    @Html.ActionLink("Modifica", "Edit", new { id = item.idAnimale }) |
                    @Html.ActionLink("Visite", "Index", "Visite", new { idAnimale = item.idAnimale }, null)
                </td>
            </tr>
        }

    </table>*@