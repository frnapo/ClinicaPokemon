@model IEnumerable<ClinicaPokemon.Models.Cassetti>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Armadietti.idArmadietto)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NumeroCassetto)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Armadietti.idArmadietto)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NumeroCassetto)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idCassetto }) |
            @Html.ActionLink("Details", "Details", new { id=item.idCassetto }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idCassetto }) |
            @Html.ActionLink("Prodotti", "Index", "Prodotti", new { id=item.idCassetto }, null)
        </td>
    </tr>
}

</table>
